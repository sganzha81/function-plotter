# Function Plotter

A small Python learning project that plots a mathematical function.

Current version: plots y = x^2.

## How to run

Install dependencies:

```bash
python3 -m pip install -r requirements.txt